# SOPS secrets for lumquat
# These values should be filled in with actual secrets
secrets:
  tailscale-auth-key:
    data: ""
    # Fill in with: tailscale auth key from https://login.tailscale.com/admin/settings/keys
