[credential]
    helper = cache

[user]
    # TODO: add a secrets system and use it here so that we aren't storing keys
    # in github or the nix store
    # signingkey = TBD

[gpg]
    format = ssh

[gpg "ssh"]
    # program = "C:/Users/A Shebanow/AppData/Local/1Password/app/8/op-ssh-sign.exe"

[commit]
    # gpgsign = true
